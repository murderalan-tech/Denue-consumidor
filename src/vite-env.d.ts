/// <reference types="vite/client" />

// Identificador de build inyectado por vite.config.ts (define), usado para
// detectar cuando hay una versión nueva desplegada (ver App.tsx).
declare const __APP_VERSION__: string;
